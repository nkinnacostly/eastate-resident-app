/**
 * Generated Supabase types.
 *
 * `src/types.ts` is GENERATED — never hand-edit it. Regenerate after every
 * migration:
 *
 *     npm run db:types          # against the linked remote project
 *     npm run db:types:local    # against the local stack (requires Docker)
 *
 * With no API layer between the clients and Postgres, this file is the only
 * thing that catches contract drift at compile time. A stale types.ts silently
 * removes that safety net.
 */
export type { Database, Json } from './types';
